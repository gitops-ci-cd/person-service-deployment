apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    environment: sandbox
  name: person-service
spec:
  selector:
    matchLabels:
      environment: sandbox
  template:
    metadata:
      labels:
        environment: sandbox
